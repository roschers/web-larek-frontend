# Проектная работа "Веб-ларек"

Проект "Веб-ларек" — веб-приложение для онлайн-покупок. Архитектура проекта построена по принципу **MVP**

Стек: HTML, SCSS, TS, Webpack, Внешний API, EventEmitter

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Установите [Node.js]

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка


```
npm run build
```

или

```
yarn build
```

## 🏗️ Архитектура приложения

### Слои приложения

1. **`Model`:** -	Управление данными (товары, корзина, заказы), бизнес-логика, работа с API.
2. **`View`:** -	Отображение данных, обработка событий интерфейса. 
3. **`Presenter`:** -	Связь Model и View через EventEmitter.

### Взаимодействие слоёв

Пользователь → View (клик) → EventEmitter → Presenter → Model → Обновление данных → View

---

## Основные компоненты

### Базовые классы
---
**Класс `Api` (Базовый HTTP-клиент)**
Методы: 
* `get` - GET-запрос.
* `post` - POST/PUT-запрос с JSON-телом.

**Класс `myApi` (наследует `Api` и выполняет интерфейс `IApi`)**
Методы:
* `getCardItem` — получает данные о товаре по ID.
* `getCardList` — возвращает список всех товаров.
* `orderItems` — отправляет заказ на сервер.

**Класс `EventEmitter` (Брокер событий)**
Методы:
* `on` — подписка на событие.
* `emit` — генерация события.
* `off` — отмена подписки.
* `onAll` - слушать все события.
* `offAll` - сбросить все обработчики.

**Класс `Component<T>` (Абстрактный класс для компонентов View)**
Методы:
* `toggleClass` — управление CSS-классами.
* `setText` — изменение текста элемента.
* `setImage` — установка изображения.
* `setDisable` - Изменяет статус блокировки элемента, делая его доступным или недоступным. 
* `setVisible` - контролирует видимость, отображает элемент.
* `setHidden` — контролирует видимость, скрывает элемент.
* `render` — генерация DOM-элемента.
* `getButtonStatus` - возвращает текст состояния кнопки.

**`Model<T>` (Абстрактный класс для моделей)**
Методы:
* `emitChanges`(event, payload) — уведомление об изменениях.

### Слой `Model`
---
#### Класс `AppData` (наследует `Model<IAppState>`).

**Назначение:** Управление состоянием приложения (данные каталога, корзины, заказа и ошибок форм).

**Методы:**
* `toggleBasket` — добавляет/удаляет товар из корзины, в зависимости от состояния с помощью методов addToBasket и removeFromBasket.
* `addToBasket` - добавляет товар в корзину.
* `deleteFromBasket` - удалить товар из корзины.
* `clearBasket` - очищает корзину.
* `clearOrder` - очищает данные заказа.
* `getTotalPrice` — возвращает сумму корзины.
* `getCountBasketProducts` - возвращает кол-во товаров в корзине.
* `getCardIndex` - возвращает индекс карточки в корзине.
* `validateOrder` — проверка валидности данных заказа.
* `setPreview` — устанавливает товар для предпросмотра.
* `setCatalog` — Обновляет список товаров в каталоге и отправляет событие о его изменении.
* `setOrderFields` — Заполняет поля заказа: электронная почта, телефон, адрес доставки и способ оплаты.
* `setOrderPayment` — Определяет выбранный способ оплаты (например, карта или наличные).
* `setOrderAddress` — Устанавливает адрес доставки для заказа.
* `setOrderPhone` — Вносит номер телефона, связанный с заказом.
* `setOrderEmail` — Указывает адрес электронной почты для связи.
* `setBasketToOrder` — Переносит товары из корзины и их общую стоимость в заказ.

### Слой `View`
---

#### Класс `PageView`

**Описание:** Интерфейс главной страницы.

**Методы:** 
* `counter` - кол-во товаров в корзине, обновляет счетчик.
* `gallery` - заменяет элементы каталога на новые, из массива.

#### Класс `CardView`

**Описание:** Показывает характерикисти карточки товара, название, цена, изображение и тд.

**Методы:** 
* `id` - идентификатор товара.
* `title` - названия товара.
* `description` - описания товара.
* `price` - цена товара, если цена не указана пишет "Бесценно" и отключает кнопку покупки.
* `image` - изображение товара.
* `category` - категория товара.
* `button` - текст кнопки.

#### Класс `ModalView`

**Описание:** Управляет модальными окнами (корзина, детали товара, формы заказа).

**Поля класса:**
* `_closeButton` - DOM-элемент кнопки закрытия модального окна, который реагирует на нажатие для закрытия окна.
* `_content` - DOM-элемент, предназначенный для отображения содержимого модального окна.

**Методы:**
* `content` - Устанавливает новое содержимое модального окна.
* `open` - открывает модальное окно.
* `close` - закрывает модальное окно.
* `render` - отрисовывает модальное окно с переданными данными, вызывая метод open для отображения окна.

#### Класс `FormView`

**Описание:** отвечает за поведение и отображение формы.

**Методы:**
* `valid` - состояние доступности кнопки отправки.
* `errors` - текст ошибки в контейнере форм.
* `render` - обновляет состояние формы.
* `clearValue` - очистка формы.

#### Классы `ContactsView` / `OrderView`

**Описание:** формы ввода данных. Валидация полей перед перед переходом между шагами.

**Методы `ContactsView`:** 
* `email` - поле для электронной почты.
* `phone` - поле для номера телефона.

**Методы `OrderView`:** 
* `address` - Адрес.
* `togglePayButton` - активирует кнопку способа оплаты.
* `clearPayButton` - очищает активное состояние для всех кнопок.

#### Класс `DoneView`

**Описание:** отвечает за состояние когда заказ успешно офрмлен.

**Поля класса:**
* `close` - закрывает модальное окно.
* `description` - текст "Заказ оформлен".

**Методы:**
* `total` - сумма итогового заказа.

#### Класс `BasketView`

**Описание:** отвечает за отображение и упраление корзиной товаров.

**Поля класса:**
* `_list` - все добавленые товары в корзину.
* `_total` - обновляет значение содержимого корзины.
* `_button` - переход к следующему этапу оформления заказа.

**Методы:**
* `items` - обновляет содержимое корзины.
* `total` - обновляет общую сумму товара в корзине.

#### Класс `CardBasketView`

**Описание:** расширение для класса CardView. Предназначен для отображения индекса товара, кнопки удалить, так же отображает инфлрмацию о товаре.

**Поля класса:**
* `_index` - отображается порядковый номер в корзине.
* `_title` - название товара.
* `deleteButton` - удаление товара.

**Методы:**
* `index` - устанавливает и отображает порядковый номер в корзине.

### Слой `Presenter `
---
Реализован через **`EventEmitter`:**

**Ключевые события:**

* `card:select` — выбор товара для просмотра.
* `cards:changed` - изменение каталога товаров.
* `card:basket` - выбор карточки в каталоге.
* `basket:open` - открыить корзину.
* `basket:add` — добавление товара в корзину.
* `basket:changed` - изменение данных в корзине.
* `modal:open` - открыть модальное окно.
* `modal:close` - закрыть модальное окно.
* `order:open` - открытие формы заказа.
* `order:changed` - изменение в форме заказа.
* `order:submit` — оформление заказа.
* `formerrors:changed` - валидация полей.
* `preview:changed` - изменение превью карточки.

## Описание интерфейсов 

### Базовые интерфейсы 
---
1. **`IEventEmitter`** - Описывает систему событий для взаимодействия компонентов.
2. **`IApi`** - Определяет методы для работы с API.
3. **`IAppState`** - Хранит глобальное состояние приложения.
4. **`IPageView`** - Управляет отображением главной страницы.
5. **`ICardView`** - Описывает данные карточки товара.
6. **`IModalData`** - Управляет модальными окнами.
7. **`IBasketView`** - Описывает состояние корзины.

#### Типы данных

8. **`CardType`** - Категории товаров.
9. **`CardBasketView`** - Упрощенное представление товара в корзине.
10. **`FormErrors`** - Ошибки валидации полей формы заказа.

#### Заказ и оплата

11. **`IOrderView`** - Данные для оформления заказа.
12. **`IOrderViewResult`** - Результат успешного оформления заказа.

#### Взаимодествие с UI 

13. **`ICardViewActions`** - Обработчики событий для карточки товара.
14. **`IDoneViewActions`** - Действия для завершения заказа.

#### Абстрактные классы

15. **`IComponent<T>`** - Описывает базовые методы для UI-компонентов (управление классами, текстом, видимостью).  
16. **`IModel<T>`** - Определяет методы для моделей данных (уведомление об изменениях состояния).
